spring:
  application:
    name: retail-analytics-app
  liquibase:
    enabled: false
  datasource:
    url: "jdbc:h2:mem:testdb"
  redis:
    url: "redis://localhost:6379"
  jpa:
    show-sql: true
retail:
  favorites:
    top:
      count: 3